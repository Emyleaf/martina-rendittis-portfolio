# Repository instructions

- Preserve the user's existing local changes when editing this project.
- In `index.html`, the image `./img/food-castelvecchio.jpg` inside the mobile Food card must retain the exact inline declaration `style="scale: 110%;"`. It crops an unwanted black strip.
- In `styles.css`, keep the image-specific `img[src="./img/food-castelvecchio.jpg"]` fallback declaration `scale: 110% !important;` when making unrelated layout or styling changes.
